# requirements_fixed.txt - Docker-compatible with build fixes

# Core Dependencies (Stable versions)
pyrogram==2.0.106          # ✅ Latest stable version
tgcrypto>=1.2.5            # ✅ Cryptography acceleration 
python-dotenv==1.0.0       # ✅ Environment variables

# HTTP and Download Optimization 
aiohttp>=3.9.5             # ✅ Security update - fixes CVE-2024-23334
aiofiles==23.2.1           # ✅ Async file operations

# FFmpeg Integration
ffmpeg-python              # ✅ FFmpeg Python wrapper

# HTTP Client (fallback)
requests                   # ✅ Sync HTTP client

# Performance Optimizations (Linux only - conditional)
uvloop>=0.17.0; sys_platform != "win32"     # 🚀 2-4x faster asyncio (Linux/macOS)

# Character Detection (Fixed alternative to cchardet)
charset-normalizer>=3.0.0  # ✅ Better alternative to cchardet (pure Python, no gcc needed)
